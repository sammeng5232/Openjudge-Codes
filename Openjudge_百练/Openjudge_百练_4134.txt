百练-4134:查找最接近的元素

总时间限制: 1000ms 内存限制: 65536kB
描述
在一个非降序列中，查找与给定值最接近的元素。

输入
第一行包含一个整数n，为非降序列长度。1 <= n <= 100000。
第二行包含n个整数，为非降序列各元素。所有元素的大小均在0-1,000,000,000之间。
第三行包含一个整数m，为要询问的给定值个数。1 <= m <= 10000。
接下来m行，每行一个整数，为要询问最接近元素的给定值。所有给定值的大小均在0-1,000,000,000之间。
输出
m行，每行一个整数，为最接近相应给定值的元素值，保持输入顺序。若有多个值满足条件，输出最小的一个。
样例输入
3
2 5 8
2
10
5
样例输出
8
5

#include <bits/stdc++.h>
using namespace std;

int n, m, x, a[100005];

int bnysearch(int l, int r) {
  int mid;
  while (l <= r) {
    mid = l + (r - l) / 2;
    if (a[mid] == x)
      return a[mid];
    else if (a[mid] < x)
      l = mid + 1;
    else
      r = mid - 1;
  }
  return l ? (abs(a[l] - x) < abs(a[l - 1] - x) ? a[l] : a[l - 1]) : a[l];
}

int main() {
  cin >> n;
  for (int i = 0; i < n; i++)
    cin >> a[i];
  cin >> m;
  for (int i = 0; i < m; i++) {
    cin >> x;
    cout << bnysearch(0, n - 1) << "\n";
  }
  /*
  for (int i = 0; i < m; i++) {
    cin >> x;
    if (*lower_bound(a, a + n, x) == a[n])
      cout << a[n - 1] << "\n";
    else if (*lower_bound(a, a + n, x) == a[n - 1]) {
      if (abs(a[n - 2] - x) <= abs(a[n - 1] - x))
        cout << a[n - 2] << "\n";
      else
        cout << a[n - 1] << "\n";
    } else if (*lower_bound(a, a + n, x) == a[0])
      cout << a[0] << "\n";
    else if (*lower_bound(a, a + n, x) == a[1]) {
      if (abs(a[0] - x) <= abs(a[1] - x))
        cout << a[0] << "\n";
      else
        cout << a[1] << "\n";
    } else {
      if (abs(*(lower_bound(a, a + n, x) - 1) - x) <=
          abs(*lower_bound(a, a + n, x) - x))
        cout << *(lower_bound(a, a + n, x) - 1) << "\n";
      else
        cout << *lower_bound(a, a + n, x) << "\n";
    }
  }*/
  return 0;
}
